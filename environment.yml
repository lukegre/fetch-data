name: fetch-data
channels:
  - conda-forge
dependencies:
  # Required for full project functionality (don't remove)
  - pytest
  # Examples (remove and add as needed)
  - numpy
  - pooch
  - joblib
  - fsspec
  - pip:
    - python-dotenv
    - envyaml
    - requests
    - aiohttp
